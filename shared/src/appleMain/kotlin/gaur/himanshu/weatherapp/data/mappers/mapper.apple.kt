package gaur.himanshu.weatherapp.data.mappers

import io.ktor.client.plugins.DataConversion
import org.koin.core.time.TimeInMillis

actual fun formatDate(millis: Long) {
    //val data = DataConversion(formatDate(millis))

}